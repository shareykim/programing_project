
// Assignment02_1
/*
#include <stdio.h>

int main() {
	int i, bacteria_num, growth;   // 변수 i, bacteria_num, growth를 정수형으로 선언한다.
	i = 0;                         // while문을 실행할 때 필요한 루프 제어 변수를 선언하고 0을 할당한다.
	bacteria_num = 10;
	growth = 4;                    // bactera_num(박테이라 수)과 growth(증식률)에 값을 할당한다. 
	while (i < 7) {                // i의 값이 7보다 작을 때까지 반복한다(7번 반복한다).
		bacteria_num = bacteria_num * growth;        
		// 한 번 반복할 때마다 bacteria_num에 4(증식률)을 곱해 다시 bacteria_num에 할당한다.
		i++;                       
	}

	printf("7시간 후의 세균의 수는 %d마리 입니다.", bacteria_num);       // 최종값을 출력한다.
	return 0;
}
*/