/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int pay, price5000, price1000, price500, price100, g;

	printf("물건 값을 입력하시오:");
	scanf("%d", &pay);



	g = 10000 - pay;

	price5000 = g / 5000;
	g = g % 5000;

	price1000 = g / 1000;
	g = g % 1000;

	price500 = g / 500;
	g = g % 500;

	price100 = g / 100;
	
	printf("사용자가 낸 돈: 10000\n오천원권: %d장\n천원권: %d장\n오백원 동전: %d개\n백원 동전: %d개", price5000, price1000, price500, price100);

	return 0;
}


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>


int main() {
	int score;
	scanf("%d", &score);

	(score >= 70) ? printf("pass") : printf("no pass");

	return 0;
}

/// 도전문제2

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {
	double score1, score2, score_final, diff, average;

	printf("테스트 1: ");
	scanf("%lf, ", &score1);

	printf("테스트 2: ");
	scanf("%lf, ", &score2);

	average = (score1 + score2) / 2;
	diff = 

	printf()
}

/// 도전문제3 - 양념 반 후라이드 반

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int A, B, C, X, Y, fried, half, seasoned;
	scanf("%d %d %d %d %d", &A, &B, &C, &X, &Y);

	/// 양념 치킨 A원, 후라이드 치킨 B원, 반반 치킨 C원 양념 치킨 최소 X마리, 후라이드 치킨 최소 Y마리를 구매하는 비용의 최솟값

	/// 반반 치킨 2마리 -> 후라이드 치킨, 양념 치킨 1마리 씩

	fried 


}
*/
