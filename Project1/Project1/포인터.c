/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void reverse(int* start, int* end) {
	while (start < end) {
		int temp = *start;
		*start = *end;
		*end = temp;

		start++;
		end--;  // 값이 공유돼서 return문이 필요없다
	}
}

int main()
{
	int arr[] = { 1, 2, 3, 4, 5 };
	int len = sizeof(arr) / sizeof(arr[0]);

	reverse(arr, arr + len - 1);

	for (int i = 0; i < len; i++) {
		printf("%d", arr[i]);
	}
	return 0;
}
*/

#include <stdio.h>
int main() {
	int 
}
